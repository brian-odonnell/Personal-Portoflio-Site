const Helpers = {
	/**
	 * 
	 * @param {number} x First Digit to Add
	 * @param {number} y Second Digit to Add
	 * @returns x + y
	 */
	AddTwoNumbers(x, y) {
		return x + y;
	}
};

export { Helpers };